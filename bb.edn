{:paths ["."]
 :tasks
 {:requires ([build :as b])
  clean
  {:doc  "Remove all build files"
   :task (b/delete-build-files)}
  download
  {:doc  "Download the latest tarball"
   :task (b/download-latest-tarball)}}}
